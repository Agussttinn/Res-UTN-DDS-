DB_NAME=resutn_db
DB_USER=resutn_user
DB_PASSWORD=tu_password_local
DB_HOST=localhost
DB_PORT=3306

# --- Opcionales en desarrollo (tienen valores por defecto) ---
# En producción SECRET_KEY es OBLIGATORIA (firma los tokens de login). Generar una con:
#   python -c "from django.core.management.utils import get_random_secret_key as k; print(k())"
# SECRET_KEY=
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1
# SYSACAD_API_URL=http://localhost:4000
